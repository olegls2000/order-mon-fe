import { HighligthCompletedTodoDirective } from './highligth-completed-todo.directive';

describe('HighligthCompletedTodoDirective', () => {
  it('should create an instance', () => {
    const directive = new HighligthCompletedTodoDirective();
    expect(directive).toBeTruthy();
  });
});
